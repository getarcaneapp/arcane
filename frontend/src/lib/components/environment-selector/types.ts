// Re-export everything from context for backwards compatibility
export {
	type EnvironmentFilterState,
	type InputMatch,
	type Suggestion,
	type EnvironmentGroup,
	type Environment,
	type EnvironmentFilter,
	type TagMode,
	type StatusFilter,
	type GroupBy,
	defaultFilterState
} from './context.svelte';
