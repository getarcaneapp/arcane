{
	"name": "arcane-tests",
	"type": "module",
	"scripts": {
		"preinstall": "npx only-allow pnpm",
		"check": "tsc --noEmit",
		"format": "vp fmt",
		"format:check": "vp fmt --check",
		"test": "playwright test"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"@types/node": "^26.2.0",
		"typescript": "^7.0.2",
		"vite": "catalog:",
		"vite-plus": "catalog:"
	}
}
