{
	"name": "arcane-tests",
	"type": "module",
	"scripts": {
		"preinstall": "npx only-allow pnpm",
		"check": "tsc --noEmit",
		"format": "oxfmt",
		"format:check": "oxfmt --check",
		"test": "playwright test"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"@types/node": "^26.2.0",
		"oxfmt": "^0.62.0",
		"typescript": "^6.0.3"
	}
}
